#/bin/sh

find . -name "newrelicConfig*.jar" -exec echo {} \; -exec jar tf {} \;
echo
find . -name *mapping.txt -exec grep -l NR_B {} \; -exec grep NR_B {} \;
echo
find . -name ".metadata" -exec echo {} \; -exec cat {} \; -exec echo \;
